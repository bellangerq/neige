<div class="navigation">
  {{#if @blog.icon}}
    <a class="item blog-icon" href="/"><img src="{{@blog.icon}}" alt="{{@blog.title}}" title="{{@blog.title}}" /></a>
  {{/if}}
  {{#foreach navigation}}
    <a class="item" href="{{url absolute="true"}}">{{label}}</a>
  {{/foreach}}
</div>
